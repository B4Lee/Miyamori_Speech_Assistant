# Miyamori_Speech_Assistant

Python app that uses speech recognition and text-to-speech

### Dependencies

```
pip install PyAudio

For Windows OS = pip install pipwin --> pipwin install pyaudio 
```

```
pip install speechrecognition
pip install pyttsx3
pip install pyaudio
pip install playsound
```
